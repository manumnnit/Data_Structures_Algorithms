#include <stdio.h>
// compiler version
//string problem
int main()
{
    char para[2000000],*ch;
    scanf("%[^\t]s",para);
    ch=para;
   
    
    //printf("%s",para);
    return 0;
}
